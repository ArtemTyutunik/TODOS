import React from 'react';

const NewAvatarSet = () => {
  return (
    <div>
      {/* eslint-disable-next-line react/no-unescaped-entities */}
        New avatar set. Reload the page in case it hasn't updated automatically
    </div>
  );
};

export default NewAvatarSet;
